#include <iostream>
using namespace std;
int main () {

    //  Q1. Take positive integer input and tell if it is divisible by 5 or not.
    // int a;
    // cout << "Enter a positive integer: ";
    // cin >> a;
    // if (a % 5 == 0) cout << "The integer is divisible by 5";
    // else cout << "The integer is not divisible by 5 ";
    // return 0;


    
    // Q2. Take length and breadth of rectangle as input and write a program to find whether the area of rectagle is greater than its perimeter.
    // int length;
    // int breadth;
    // cout << "Enter the length of rectangle: ";
    // cin >> length;
    // cout << "Enter the breadth of rectangle: ";
    // cin >> breadth;
    // int area = (length*breadth);
    // int perimeter = 2*(length + breadth);
    // if (area > perimeter) cout << "Area is greater than the perimeter for the given rectangle";
    // else if (area == perimeter) cout << "Area is equal to the perimeter for the given rectangle ";
    // else cout << "Perimeter is greater than the area of the given rectangle";
    // return 0;

}