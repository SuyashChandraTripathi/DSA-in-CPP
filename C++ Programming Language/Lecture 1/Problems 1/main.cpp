#include <iostream>
using namespace std;
int main () {

// Q1.    Take an integer n as input. Print the value of 4n +9.
//     Sample — Input: 5 ⇒ Output: 29
    //    int n;
    //    cout << "Enter an integer: ";
    //    cin >> n;
    //    cout << 4*n+9 << endl;


//  Q2. Take four integers a, b, c, d as input. Compute and print (a +b) ×(c+d).
//  Sample — Input: 1 2 3 4 ⇒ Output: 21
    // int a,b,c,d ;
    // cin >> a >> b >> c >> d ;
    // cout << (a+b) * (c+d);
    // return 0;


// Q3. Write a program to calculate the Volume of a Sphere given radius r. Use π = 3.14.
//                      V = 4*pi*r^3
// Sample — Input: 3 ⇒ Output: 113.04
    //   int radius ;
    //   cout << "Enter the radius of the Sphere: ";
    //   cin >> radius ;
    //   cout << ((float)4/3)*(3.14)*(radius)*(radius)*(radius) << " is the volume of the given sphere.";
    //   return 0; 


// Q4. A circle of radius r is inside a square of side a (the circle always sits completely inside
// the square without touching its edges). Find the area of the shaded region — the part inside
// the square but outside the circle. Use π = 3.14. (Figure given in the DPP)
    //    int r , a;
    // cout << "Enter the radius of the cirlce: ";
    // cin >> r ;
    // cout << "Enter the side of the sqaure: ";
    // cin >> a ;
    // cout << ((a*a)-(3.14*r*r)) << " is the area of the shaded region.";
    // return 0;


// Q5. Take a char as input and print its ASCII value.
    // char a ;
    // cout << "Enter a character: ";
    // cin >> a;
    // cout << (int)a;
    // return 0;


// Q6. Take an integer n as input. Print the minimum non-negative integer that must be added
// to n to make it exactly divisible by 7. Use the modulus operator.
    //  int n;
    //  cout << "Enter any integer: ";
    //  cin >> n ;
    //  cout << 7-(n%7) << " is the minimum non-negative integer that must be added to " << n << " to make it perfectly divisible by 7.";
    //  return 0;


// Q7. Take two integers a and b (b is not equal to 0) as input. Print the result of ceiling integer division
// of a by b.
// Ceiling division always rounds up — e.g., ⌈7/2⌉ = 4, whereas normal integer division gives 3.
// Hint- Formula for positive integers: (a + b - 1)/b.
    //  int a , b ;
    //  cout << "Enter the first integer: ";
    //  cin >> a ;
    //  cout << "Enter the second integer: ";
    //  cin >> b;
    //  cout << "Ceiling integer division of a by b is " << (a + b - 1)/b << endl;
    //  return 0;
    

// Q8. Compute the Kinetic Energy of a moving object. Take the Mass m (in kg, double) and
// Velocity v (in m/s, double) as input from the user. Be careful about integer vs. double division
// when applying the fraction coefficient.
    //   double mass , velocity ;
    //   cout << "Enter the mass (in kg): ";
    //   cin >> mass ;
    //   cout << "Enter the velocity (in m/s): ";
    //   cin >> velocity ;
    //   cout << "Kinetic energy for this moving object is " << ((double)1/2)*mass*velocity*velocity << endl;
    //   return 0;


// Q9. Take an amount in Rupees (double) and convert it to Dollars, given that 1 Dollar =
// 100 Rupees.
    // double amt ;
    // cout << "Enter the amount (in Rs): ";
    // cin >> amt ;
    // cout << amt << " Rs is equal to " << amt/100 << " Dollars" << endl;
    // return 0;


// Q10. A shopkeeper applies a discount of d% on the marked price m, and then charges a tax of
// t% on the discounted price. Take m, d, and t (all doubles) as input and print the final amount
// the customer pays (formulas given in the DPP).
    //   double m , d ,t ;
    //   cout << "Enter the market price: ";
    //   cin >> m ;
    //   cout << "Enter the rate of discout (in %): ";
    //   cin >> d;
    //   cout << "Enter the rate of taxation (in %): ";
    //   cin >> t;
    //   cout << "----------------------------------------------------------------" << endl;
    //   cout << "The market price of this product is " << m << " Rs" << endl;
    //   cout << "Discounted price of the product is " << (m)-((d/100)*m) << " Rs" << endl;
    //   cout << "Final price the customer has to pay for this product is: " << (((m)-((d/100)*m))*(1+(t/100))) << " Rs" << endl;
    //   return 0;

}