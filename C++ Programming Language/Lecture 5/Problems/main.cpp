#include <iostream>
using namespace std;
int main (){

    // Take positive integer input and tell if it is a four digit number or not.
    // int a;
    // cout << "Enter a positive integer : ";
    // cin >> a ;
    // if (a >= 1000 && a <= 9999) cout << "This is a four digit positive integer";
    // else cout << "This is not a four digit positive integer";
    // return 0 ;

    //  Take positive integer input and tell if it is divisible by 5 or 3.
    // int a;
    // cout << "Enter a positive integer: ";
    // cin >> a ;
    // if (a % 5 == 0 || a % 3 == 0) cout << "This integer is divisible by 5 or 3";
    // else cout << "Conditions not met";
    // return 0;

    // Take 3 positive integers as input and tell if they can be the sides of a triangle or not 
    // int s1 , s2 , s3;
    // cout << "Enter the length of the first side:";
    // cin >> s1;
    // cout << "Enter the length of the second side:";
    // cin >> s2;
    // cout << "Enter the length of the third side:";
    // cin >> s3;
    // if ((s1+s2>s3) && (s2+s3>s1) && (s1+s3>s2)) cout << "The given diagram is a triangle";
    // else cout << "The given diagram is not a triangle";
    // return 0;


    //  Take 3 positive integers as input and print the greatest of them.
    // int a,b,c;
    // cout << "Enter the first integer: ";
    // cin >> a;
    // cout << "Enter the second integer: ";
    // cin >> b;
    // cout << "Enter the third integer: ";
    // cin >> c;
    // if (a > b && a > c) cout << a << " is the greatest integer among all three";
    // else if (b > a && b > c) cout << b << " is the greatest integer among all three";
    // else cout << c << " is the greatest integer among all three";
    // return 0;


    // Take positive integer input and print:
    // 'The number is divisible by 5' if number is divisible by 5 only.
    // 'The number is divisible by 3' if number is divisible by 3 only.
    // 'The number is divisible by both 3 and 5' if number is divisible by both 3 and 5.
    // 'The number is neither divisible by 5 nor 3' if number is not divisible by 5 or 3.
    // int num ;
    // cout << "Enter a number: ";
    // cin >> num;
    // if (num % 5 == 0 && num % 3 == 0) cout << "The number is divisible by both 3 and 5";
    // else if (num % 5 != 0 && num % 3 != 0) cout << "The number is neither divisible by 5 nor 3";
    // else if (num % 5 == 0) cout << "The number is divisible by 5";
    // else if (num % 3 == 0) cout << "The number is divisible by 3";
    // return 0;

}